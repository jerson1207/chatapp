// app/javascript/application.js

import { initializeDropdowns } from './dropdown'; // Import the initializeDropdowns function

// Initialize dropdowns when the document is ready
$(document).ready(function() {
  initializeDropdowns();
});
